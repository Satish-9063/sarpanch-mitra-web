# Copy to .env.local for local development. Never commit real values here.
# All production credentials live in GCP Secret Manager only (org policy).

# GCP project (sarpanch-mitra-web, pending OI-W06 approval)
NEXT_PUBLIC_GCP_PROJECT_ID=

# Headless CMS (selection pending OI-W04)
CMS_API_URL=
CMS_API_TOKEN=

# ESP / newsletter (selection pending OI-W03)
ESP_API_KEY=

# Analytics (GA4 vs privacy-first alternative — pending OI-W05)
NEXT_PUBLIC_ANALYTICS_ID=

# WhatsApp click-to-chat number (international format, no +, e.g. 919876543210)
# ⚠ System Admin: confirm the real number before launch — placeholder only.
NEXT_PUBLIC_WHATSAPP_NUMBER=
